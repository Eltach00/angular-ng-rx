export enum Urls {
  users = 'api/users',
  globalFeed = 'api/articles',
  tags = 'api/tags',
  favorite = '/favorite',
  comments = '/comments',
  login = 'api/users/login',
  user = 'api/user',
  follow = '/follow',
  profiles = 'api/profiles',
}
